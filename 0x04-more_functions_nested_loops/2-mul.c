#include "main.h"

/**
 * mul - function to multiply 2 integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: always 0
 */
int mul(int a, int b)
{
	int result;

	result = ((a) * (b));

	return (result);
}
