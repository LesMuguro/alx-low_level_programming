#include "main.h"

/**
 * positive_or_negative - function to check if a number is positive
 * or negative
 *
 * @i: the number to be checked
 *
 * Return: no return value
 *
 */
void positive_or_negative(int i)
{
	if (n > 0)
	{
		printf("%d is positive\n", n);
	}
	else
	{
		printf("%d is negative\n", n);
	}
}
