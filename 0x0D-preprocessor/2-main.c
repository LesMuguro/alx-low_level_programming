#include <stdio.h>
#include <stdlib.h>

/**
 * main - function to print the name of the name of the file
 * it was compiled from, followed by a new line
 *
 * Return: always 0
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
