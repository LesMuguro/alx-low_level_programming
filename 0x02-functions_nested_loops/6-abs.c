#include "main.h"

/**
 * _abs - function to find the absolute value (Modulus)
 * of a number
 *
 * @n: character in ASCII code
 *
 * Return: value of n and always 0
 *
 */
int _abs(int n)
{
	return (n * ((n > 0) - (n < 0)));

	return (0);
}
